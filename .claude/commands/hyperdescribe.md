---
description: Prune comments to near-zero and rewrite the code hyperdescriptive instead, behind a build+tests-green safety invariant.
argument-hint: "[file-or-directory]"
---

# hyperdescribe: $ARGUMENTS

Load and follow `.claude/skills/hyperdescribe/SKILL.md`. Scope the sweep to `$ARGUMENTS` when given; otherwise resolve the next unswept target per the skill's step 1.
