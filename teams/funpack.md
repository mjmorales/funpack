---
schema_version: 2
team:
  slug: funpack
  team_type: stream_aligned
  charter: "Owns the funpack compiler — grammar, frontend, semantics, artifact emission, Index Contract"
  lifetime: persistent
  terminates_on_milestone: null
  status: active
  created_at: 2026-06-05T02:40:25.168Z
scope:
  read: ["**"]
  write: ["funpack/**"]
roster:
  tech_lead: ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8
  engineer: ct-semantics-seat-c17dfd55-65eb-42c6-8b53-81404a81d88e
  implementer: ct-frontend-seat-c5a96c18-734a-4c61-883d-85ed54027522
interface:
  accepts: []
  exposes:
    - { name: "index-contract", schema_ref: "funpack-spec: Index Contract — schema-versioned, exact-match NDJSON" }
lore:
  count: 13
  live: 13
  recent:
    - { id: 01KVXX1QYS5ATDF53BT8NMHEM2, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-24T22:47:10.553Z", body: "let TUPLE-DESTRUCTURE is now legal (ADR 2026-06-24-let-tuple-destructure-binding): let (a, b, ...) = expr binds each name to a return-position tuple element — the threaded-Rng consume site let (value, next) = draw(rng). This was a frontend regression AGAINST spec doctrine, not a doctrine gap: spec 02 section 8 ALREADY named \"the let/match that destructures such a [return-position tuple]\" as canonical; only the grammar/fun.ebnf LetStmt production + the section-6 cheatsheet had failed to carry the let half. THE GRAMMAR SEAM (LL(1) preserved): a ( immediately after let is the one-token signal selecting parse_let_tuple over the single-name form, no backtrack — the former Let_Tuple_Destructure PARSE-error arm flipped from reject to parse a binder list. The binder list is FLAT plain lower-idents (snake_case), NOT the nested match-pattern grammar — structural matching stays in match; a refutable pattern in an irrefutable let binding was rejected (reintroduces exhaustiveness). TYPECHECK: bind_let_tuple zips each binder against the RHS ^Tuple_Type element; a count/non-tuple mismatch is the new closed-enum Type_Error Let_Tuple_Arity_Mismatch (named at the destructure seam, not a generic Type_Mismatch). LANDMINE worth the lore: bind into scope BY POINTER (^Scope), never a by-value map copy — an Odin map grown by-value reallocates only the LOCAL header, so a by-value bind flaked a later-statement Unresolved_Name (non-deterministic, only when the scope map happened to resize). The deliberate spec lives in the threaded-Rng determinism junction (rand_golden_test.odin), proving let-threaded draws equal the one-deep match-form seed-42 goldens. THE DEFERRED CROSS-TEAM SEAM (tracked: v19-artifact-reshape-carry-let-mqsnxil9): the v18 wire format encodes a let as ONE name scalar; the N-name binder list needs a v19 funpack<->runtime reshape (exact-match schema, runtime owns the decoder/interp half). Until then a tuple-let in a GAMEPLAY body (fn/behavior/query) is refused before emission (let_tuple_wire_gate -> Build_Error.Let_Tuple_Unsupported_Wire, exit 2, actionable message) rather than emitting an undecodable node — honoring the purity boundary AND avoiding the dual-interpreter parity trap. The form is fully usable+tested in test blocks today (off the wire, via the compiler evaluator). Rejected alternative: keep it banned and only fix the skills — leaves every threaded engine.rand call site needing a data-wrapper shim (the report workaround) and the structural trap intact." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHY, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-14T05:10:41.621Z", body: "Diagnostics are now a FIRST-CLASS COMPILER SEAM (the README fix-criteria quality-gate non-negotiable: a refusal is a rustc/gofmt block naming the offender, its file:line:col, the closed-enum rule, and a concise fix sentence — so an agent's write->check->fix loop converges). Three-layer information-loss fixed, source -> CLI.\n\nTHE SEAM (funpack/diagnostics.odin): the core Diagnostic{stage,rule,line,col,declaration,message,path} record; the PURE render_diagnostic(d, source) (a function of (Diagnostic, source) ALONE, so a golden pins it byte-for-byte: header path:line:col: rule (decl): message, then gutter-numbered excerpt + caret under col; line==0 collapses to header-only, col==0 omits the caret, an out-of-range line fails OPEN with an empty excerpt never a crash); and the FIVE per-stage mapping procs — parse_diagnostic / gate_diagnostic / type_diagnostic / contract_diagnostic / flatten_diagnostic — each a CLOSED switch over its stage error enum mapping arm -> (rule = the arm's own name (the machine-stable identifier, MUST equal the enum arm), message = the fix sentence). THOSE FIVE PROCS ARE THE SINGLE SOURCE OF TRUTH FOR HUMAN WORDING. A new enum arm is a visible compile gap in its proc (total switch), never a silently-wordless diagnostic.\n\nPROVENANCE SOURCES (where line/col come from):\n- expr_span(e: Expr) -> (line, col) in expr.odin: the TOTAL switch over the Expr union, leftmost-byte-of-the-construct anchor convention (literals on lead token; Binary_Expr on the LEAD OPERAND not the operator; Unary_Expr on the operator; Call/Member/With on callee/receiver/base; a parenthesized group passes its inner span through; only a tuple anchors on lparen). Synthetic downstream-built Expr nodes keep (0,0) = no-source-position, correct. Token carries col (1-based first byte in line) + offset (0-based byte in source), stamped by stage_lex tracking line_start.\n- decl .line (1-based source line of the lead keyword, artifact-format §9 span provenance) on Fn/Behavior/Query/Test/Data/Enum/Signal/Let nodes: the decl-line anchor for offenders that own no expression span (gate offenders, contract behaviors, flatten signals, membership-class type faults).\n\nTHE INFORMATION-LOSS, FIXED AT THREE LAYERS:\n1. STAGE SEAM — each stage keeps its coarse closed enum as the MACHINE CONTRACT and ADDS offender coordinates beside the arm: Parse gained stage_parse_located -> Parse_Verdict{err,line,col} via parser_stop_span (the token at clamp(p.pos,last) — the single-token-lookahead anchor); Gate's Gate_Unit/Gate_Verdict gained .line (decl line); Typecheck gained stage_typecheck_located -> Type_Verdict{err,line,col,declaration} via a pointer-threaded Type_Diag_Site sink on Check_Ctx.diag (expr_check stamps the INNERMOST offending expr span FIRST-WRITE-WINS; the body/test sweeps stamp the declaration via stamp_decl; statement-level rejects call stamp_expr); Contract/Flatten resolve the offender decl line from ast.behaviors/ast.signals.\n2. PIPELINE DRIVER — run_module_pipeline_diag(source,index,modules,module) returns (report, Pipeline_Error, Diagnostic): each stage seam maps (arm,line,col,declaration) through its per-stage proc into ONE Diagnostic with path=\"\". run_module_pipeline_named is its (report,err) projection so existing tests are unchanged. Project_Report.diagnostic carries it with path=failed_path; Build_Verdict.diagnostic threads the inner diagnostic on Compile_Failed ONLY (stage_build recovers the first-failing-module Diagnostic via run_project_pipeline — emit/index only know a coarse floor tripped).\n3. CLI — eprint_module_diagnostic (test verb) + eprint_build_refusal (build/check) re-read diag.path and render; non-Compile_Failed Build_Error arms keep build_refusal_message.\n\nTHE MACHINE-CONTRACT-UNCHANGED INVARIANT (load-bearing): exit codes stay BYTE-IDENTICAL — compile error 2, failed assertions 1, all-pass 0; a compile error is NEVER a counted failure. The Diagnostic is the ADDED HUMAN BODY beside the coarse Pipeline_Error/Build_Error class, never a replacement. Adding/widening a Diagnostic is NOT an Index-Contract reshape (the Index Contract NDJSON is untouched) and NOT a schema bump.\n\nPINNED GOLDENS (funpack/diagnostics_test.odin): the rendered block is pinned BYTE-FOR-BYTE for one representative arm of EACH stage through run_module_pipeline_diag (Parse=Wrong_Case, Gate=Arity_Exceeded, Typecheck=Type_Mismatch, Contract=Render_Emits, Closure=Unclosed_Signal) plus the membership decl-line pin. Each source is a small self-contained snippet tripping exactly its stage's FIRST offender (first-offender determinism), so identical source => identical bytes. These goldens stop diagnostic quality from silently regressing: a wording edit, a span-anchor shift, or a gutter/caret-shape change moves the bytes and fails.\n\nTWO PRECISION GAPS, RESOLVED. (a) Wrong_Case caret off-by-one DEFERRED (task tighten-parser-reject-span-wro-mqdbq3w8): parser_stop_span anchors at p.pos, which a post-advance casing reject has already moved past the offending identifier, so the caret lands one token late (col 14 the '{' not col 7 'widget' for 'thing widget {'). NOT a one-line fix — it ramifies across 32 Wrong_Case sites + the post-advance pattern across ~203 or_return sites; the correct fix is a parser-wide reject-span discipline, its own bounded change. The col-14 behavior is LOCKED in test_diag_pin_parse_wrong_case with a NOTE; the task's acceptance flips that golden. (b) Membership-class type checks (layer registry / @migrate / index paths / expose closure) FIXED INLINE: they own no expression span so they rendered header-only at line 0; threading the Type_Diag_Site sink into check_layer_registry/check_migrations/check_index_paths/check_expose_closure and calling stamp_decl(site, decl.name, decl.line) at each reject now anchors them on the owning declaration's line (col 0) — the SAME gate-offender decl-line shape (Expose_Closure_Verdict gained a .line field). Pinned in test_diag_pin_membership_expose_closure_anchors_decl_line.\n\nWORDING/SPEC-CITE REVIEW (every message cross-checked against its enum arm's rich doc-comment AND ../funpack-spec/spec): one fix — Wrong_Case cite tightened §02 -> §02 §1 (the casing rules live in §02 §1 Lexis 'Identifiers & casing', NOT §02 §4 Names/calling-convention). All other cites verified accurate against the spec section headers, including the non-obvious ones: Index_Unknown_Thing/Index_Unknown_Field/Malformed_Index_Path -> §05 §3 (the FieldPath-head rule + the error name are anchored there); Malformed_String_Escape -> lexical-core §4 (the closed escape set \\\" \\{ \\}); Reserved_Signal_Name -> §11 §4 (Trigger/Contact engine-routed); Unregistered_Layer -> §11 §5; the @migrate family -> §05 §6; the probe family -> §28 §4 On-table; Duplicate_Declaration + the four budget gates -> §01 P5 (which covers BOTH structural budgets AND the duplication ban).\n\nFUTURE SEATS: diagnostics are now a seam you EXTEND, not improvise. Add an enum arm -> add its mapping-proc message (the total switch forces it) with an accurate spec cite (rule string = the arm name). Add offender coordinates beside the arm (expr_span for an expression fault, decl .line for a declaration fault). The five mapping procs are the wording's single source of truth — never word a refusal anywhere else. Validate: build/lint/test all green (funpack 1257, runtime 533); the byte pins are the regression floor." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHV, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-12T23:59:08.081Z", body: "ARTIFACT_SCHEMA_VERSION v16→17 — the §19 textured-render cross-boundary links (ADR 2026-06-12-v17-textured-render-cross-boundary-links). v16 carried the sprite PIXELS but no bridge from a sprite/tile REFERENCE to them, so every real dungeon sprite/tile fail-closed to no texture (runtime Stage-2b). ONE 16→17 bump (window CLOSED at v16 per lore #20 — committed runtime/testdata pins v16) bundles THREE coupled changes; three separate bumps would each force a full testdata reconcile. (1) [assets] atlas keyed by the manifest HANDLE name — bake_atlas_assets keys Baked_Atlas.name by entry.name (dungeon_atlas, the [name] block), NOT parsed.name (DungeonAtlas, the .atlas-declared name no Draw_Sprite references): 'atlas dungeon_atlas HASH 8'. Keying by the handle name makes the manifest bake and the assets bake agree by construction (the same name the AtlasHandle const carries), so asset_region(handle,cell) resolves with no per-sprite rewrite. (2) THE ASSET-HANDLE-RESOLUTION-VIA-CONST-CARRY SEAM (emit_whole_module_lower.odin): the entrypoint reaches its handle const through a WHOLE-MODULE import (import assets, then assets.dungeon_atlas) which binds no bare name — distinct from the brace-group level-seam import (import dungeon.{terrain}) the v6/v15 carry handles. ROOT CAUSE of the silent drop CONFIRMED: the assets gen module IS collected (assets capability ON via assets/assets.manifest; the whole gen/ dir is walked for *.gen.fun, so gen/assets.gen.fun reaches sources AND build_sibling_module_asts) — the drop was NOT a collection failure, it was that the seam-fn carry (imported_user_module, emit_seam_fns.odin) REQUIRES brace-group members and import assets is memberless. The fix is the whole-module carry path. TWO mechanisms, mirroring v6/v15: CARRY — collect_whole_module_const_records carries each whole-module-referenced handle const into [functions] as a 'function NAME const' record (the v15 imported-const form, the seam module's span) with the §26 typed handle value READ from the seam AST's declared let type (AtlasHandle/SoundHandle/MeshHandle/TextureHandle — NEVER hardcoded to atlas). LOWER — lower_whole_module_refs rewrites every module.NAME member-expr in the entrypoint AST's behavior/fn/let bodies to a bare Name_Expr{NAME} (the v6 qualified→bare lowering, in-place on the fresh-per-build entrypoint AST — emit is terminal, emit_tree_artifact re-parses per call so double-build determinism holds), so the runtime resolves it by bare name with NO special-case (the operator's clean-path choice over a runtime stopgap). COLLISION GUARD (the v6 disambiguation): a whole-module const lowers to its bare name only when no own-module fn/let binds that name; a collision is Emit_Error.Whole_Module_Collision → Compile_Failed (exit 2), never two [functions] records under one bare name. Wired in stage_emit_indexed: collect const records BEFORE lowering (the carry reads the qualified refs the lowering then strips), then lower, then concat after the brace-group carries. (3) [tilemaps] per-layer atlas + per-tile cell: Tileset_Asset.atlas→Project_Tile.atlas→Baked_Tile_Layer.atlas (per-layer) and Tileset_Tile.cell_x/y→Baked_Tile.cell_x/y (per-tile). Lead line 'tilemap NAME CELL_SIZE COLS ROWS ANCHOR_X ANCHOR_Y ATLAS PALETTE_COUNT' (- for a degenerate palette-less layer); palette 'tile NAME SOLID CELL_X CELL_Y'. The runtime resolves a tile's texture through asset_region(ATLAS,(CELL_X,CELL_Y)) exactly as a sprite does. A layer whose palette mixes tilesets with DIFFERENT atlases is the Tileset_Atlas_Conflict bake gate (one atlas per layer) — refused, never silently picking one (the friction-surfaced contradiction; no committed example mixes atlases, dungeon/warren are single-tileset). NO new section, NO new sub-record KEYWORD — the atlas lead keyword and the tile/row sub-record keywords already exist; only their token shapes and the atlas record's NAME token change, plus a widened [functions] population. The 4 cross-pin tests SKIP-loudly (committed runtime copy v16 < emitter v17) — the BORN-STAGED hatch; the runtime reconcile (Stage 4) regens them to v17. Pong (asset-less/level-less) regens by the version stamp alone. funpack green (1193 tests)." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHR, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-12T22:47:24.066Z", body: "The §19 [assets] artifact section (v16 bump, ADR 2026-06-12-assets-section-base64-content-addressed) carries the decoded sprite pixels the textured Draw_Sprite{atlas,cell} needs — the FIRST artifact section whose payload is binary data, not a checked-AST projection. THREE grammar decisions. (1) base64 pixel encoding (core:encoding/base64, std RFC-4648): the canonical RGBA8 buffer (W·H·4 bytes, row-major, import_image's .alpha_add_if_missing output) -> one ASCII b64:<…> token on one line, so the committed text golden stays diffable and the runtime decodes with the same core pkg. Rejected: raw length-prefixed binary (loses text-golden diffability, fights the §2.1 LF-framed lexical frame) and hex (2× bloat for the same diffability — base64 is ~4/3). (2) content-addressed dedup by image hash: one image-record per DISTINCT content hash, atlases reference it by IMAGE_HASH, so two atlases on one sheet hold the blob ONCE. The hash is the SAME §2 content address the manifest keys the image by, so the manifest bake and the assets bake agree by construction. (3) ATLAS-ROOTED image set + grid-lowered region rects: the [assets] images are exactly the ones an atlas references (built from the atlas walk, NOT standalone manifest image entries — an unsliced image is never blitted), and region (a NEW closed sub-record keyword) derives PX_X=cell.x·grid_w, PX_Y=cell.y·grid_h, PX_W/H=grid_w/h from the atlas grid clause — so (atlas-name,cell-name)->(image pixels,pixel rect) resolves from the artifact alone. THE BAKE->EMIT PIXEL-THREADING SEAM is the load-bearing mechanism: bake_resolve_image (the manifest bake, asset_bake.odin) FREES the decoded pixels because the manifest needs only the hash; bake_tree_assets/bake_resolve_image_pixels (asset_emit_bake.odin) is its pixel-PRESERVING twin that re-decodes off disk and threads a Baked_Assets model (Baked_Image/Baked_Atlas/Baked_Region) into a NEW Emit_Input.assets field through stage_emit_indexed. emit_tree_artifact bakes it only when asset_tree_has_manifest (already staleness-gated by stage_build); an asset-less game threads the empty Baked_Assets and writes the constant [assets 0] tail (the [nav 0]/[tilemaps 0] precedent). The v16 bump CLOSES the open window per Lore #20: four cross-package byte-pins (golden_tilemap_e2e dungeon+warren, golden_krognid, golden_statequery) exact-match runtime/testdata/*.artifact, all stamped v15. They DO NOT FAIL on the funpack-side bump because each has a built-in committed_version < ARTIFACT_SCHEMA_VERSION -> SKIP-loudly hatch (the BORN-STAGED seam) — so they SKIP until the Stage-2 runtime reconcile (bump runtime->16, parse [assets], regen the four testdata copies) lands. That reconcile is the runtime team's commit; it determines commit ordering for the next stage. Pong (asset-less) regens to the v16 stamp + [assets 0] tail; the dungeon (162B PNG, 64×32 RGBA8, 8 cells) is the N>0 proof (test_golden_dungeon_build_carries_populated_assets_section: [assets 2] = 1 deduped image + DungeonAtlas with 8 region rects, round-trips through parse_artifact, double-build byte-identical)." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHQ, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-12T02:13:50.385Z", body: "ARTIFACT_SCHEMA_VERSION bump discipline — the open-window rule (ratified in ADR 2026-06-11-path-projection-rides-v15-open-window, the §8 Path projection case). The stamp pins the FORMAT (sections, record grammars, node kinds, value encodings), not the population a given source emits into those sections. Two consequences. (1) A population widening inside an already-ratified closed mechanism (the §8 synthesized engine-type [data] projections, the v6/v15 carry populations) may land under an OPEN version stamp — open meaning no committed artifact outside funpack/ pins bytes at that version (runtime/testdata byte seams staged/SKIPping, no shipped golden). Bumping anyway burns a version with zero discriminating power: every reader implements the new version identically to the old. (2) The window CLOSES the moment any cross-package byte pin lands at that version (Phase-B-style runtime reconcile, a committed testdata artifact): from then on ANY byte movement at that stamp — however precedented the mechanism — is contract drift and requires a bump. Bump when an existing reader's pinned bytes would move, never to annotate a change no reader can distinguish. Alternatives rejected for the Path case: v16 (no observable difference, doubles the staged-seam reconcile); runtime-side hardcoded field types (breaks the §8 design — the artifact carries the projection, the surface owns the type, the runtime stays schema-driven); deferring to land atomically with Phase B (ships a self-contradictory artifact whose [data] cannot type its own default tokens). This is the same boundary as the Index Contract's reshape rule: exact-match consumers define when a change is a reshape." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHP, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-08T18:54:21.320Z", body: "The §05 @stub typed-hole milestone (typed-holes-stub-release-ban) decomposes along the frontend's standing parse -> typecheck -> {index, fallback-eval, release-ban} -> fixture seam order, six independently-shippable stories. CRITICAL GRAMMAR DISTINCTION grounding the first cut: @stub is NOT a leading @doc/@gtag-style prefix directive — spec §05 §1 says it 'stands in body/expression position'. So the parse seam is NOT an extension of parse_directive's prefix loop (that handles @doc/@gtag accumulated into the Directives struct, parser.odin ~L408); it is a NEW BODY-position production — a holed fn/behavior replaces its Statement-sequence body (Fn_Node.body / Behavior_Node.step.body, parser.odin ~L164/L184) with the hole carrying the declared type T and an optional fallback Expr. That is why the AST-representation seam is the natural first story, distinct from directive-prefix grammar. (1) PARSE @stub(T)/@stub(T,fallback) into the body-bearing AST nodes — a new body-position production + the carry of the hole's declared T and optional fallback expr. Root, no in-milestone dep. (2) TYPECHECK a holed decl against its DECLARED return type so callers typecheck against the hole: the holed body short-circuits check_fn_body's statement-walk (typecheck.odin check_fn_body ~L277, expected_return = the declared -> R) — the hole's T must EQUAL the declared return; callers already typecheck against the intact signature via expr_check over Call_Expr, so caller-against-hole falls out once the signature stands and the body is the hole. Blocked by (1). (3) DERIVE the stub index field from the AST — kill the always-false: Decl_Record.stub is hardcoded false in body_less_decl/fn_decl_record/behavior_decl_record/test_decl_record (index_decl.odin) per the source_has_expose 'parser admits only @doc/@gtag' rationale, which (1) makes obsolete. NOT a schema reshape: stub is an EXISTING mandatory field flipping from constant-false to AST-derived, the field set is unchanged, so it does NOT bump INDEX_SCHEMA_VERSION. Blocked by (1). (4) @stub(T,fallback) EVALUATION — the approximation runs so the game stays playable: stage_evaluate_indexed runs fn/behavior bodies; a fallback hole evaluates its fallback Expr in the decl's env (evaluate.odin/value.odin). A typecheck-only @stub(T) with no fallback has nothing to run — dev-compiles, never shipped, a runtime reach is undefined. Blocked by (2) (the fallback must typecheck against T first). (5) --release HOLE-BAN compile error (P8 / §29 §4: you cannot ship a hole): NO --release flag exists today — main.odin dispatches build with no mode, stage_build takes no mode. This story adds the release-mode plumbing (a Build_Mode threaded build verb, mirroring asset_strip.odin's existing Strip_Mode{Dev,Release}) AND the gate: any holed decl under --release is exit-2 compile error (never a counted failure — build has no exit-1 tier). A pure-AST mode-flag verdict (§29 §1: 'pure AST or a mode flag'), so it stays on the pure side. Blocked by (1) — independent of (2)/(3)/(4). (6) GOVERNANCE FIXTURE exercising a typecheck-only @stub(T) hole + a @stub(T,fallback) hole end-to-end: authored in funpack-spec examples (NO @stub example exists there yet — grep-confirmed empty), driven through resolve_spec_dir env-override/SKIP-warn golden harness. Asserts: dev build compiles + the stub index field is true for both holes + the fallback runs + --release rejects exit 2. funpack does not grammar-include what it cannot run, so this leaf is the surface-done proof. Blocked by (3),(4),(5). OUT OF SCOPE (NOT planned): @todo expiry (deferred — open design decision) and the funpack warden query subcommands (separate milestone — this only populates the stub index field). Rejected: (a) one mega 'parse+type+index+ban' story — not independently shippable; (b) folding the release-ban into parse — the ban needs release-mode plumbing that does not exist and is a downstream mode-gate, not a grammar concern; (c) treating @stub as a parse_directive prefix arm like @doc/@gtag — WRONG per §05, it is a body/expr-position hole, the single most important grounding correction; (d) bumping INDEX_SCHEMA_VERSION for the stub-field derivation — the field already exists mandatory, only its derivation changes." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHG, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:43:08.618Z", body: "The §19 asset pipeline decomposes along the same source→importer→(content-hashed asset + .gen.fun seam) shape the frontend follows, split into five independently-shippable stories on the shared-bake-infrastructure substrate (bake-step decl, .gen.fun emitter byte contract, golden-comparison harness, seam-import path — assumed present, never re-planned). Seam order and why each cut is the natural boundary: (1) MANIFEST READER + CONTENT HASHER — the committed assets.manifest (name -> kind/source/importer-version/deps/hash/out blocks) is the source of truth for handle resolution and the closed name registry; hash = H(source bytes + importer version + dep hashes), §2. Reader+hasher is one seam: the hash rule is uniform across kinds and the manifest is the index every later seam consults. Ships behind a unit fixture over examples/assets/assets.manifest. (2) PER-FORMAT IMPORTERS (.fpm model, .atlas image+slice, raw audio) — one engine-closed deterministic importer per source kind (§4), each producing a content-hashed asset whose hash feeds the manifest; parallelizable DAG nodes (atlas deps-on its raw image hash). Distinct from the reader: the importers are the pure source→asset functions the hasher hashes OVER. (3) HANDLE-CONSTANT SEAM EMISSION + CLOSED-REGISTRY CHECK — emits assets.gen.fun byte-matching examples/assets/gen/assets.gen.fun (one typed handle constant per registered asset, lowercase folder-namespaced) via the shared .gen.fun emitter byte contract; the @gtag-for-assets P7 closed registry makes a name not in the manifest a COMPILE ERROR; string constructors mesh/atlas/sound checked against the manifest too (assets.coin_sfx == sound(coin_sfx)). Depends on the reader for the name set. (4) RELEASE DEAD-ASSET ELIMINATION + ASSET REPORT — the P5 dead-code gate applied to content (§5): release strips any asset no handle references and emits assets.report.txt byte-matching the example; a release-mode reference-graph walk over the emitted handle set, downstream of seam emission (needs handle references for reachability); dev mode bakes the dirty subgraph and never strips. (5) engine.assets SURFACE ADMISSION + GOLDEN INTEGRATION — admit engine.assets to funpack/surface.odin with EXACTLY the §26 line-78 names: types MeshHandle/TextureHandle/SoundHandle/AtlasHandle, fns mesh/texture/sound/atlas/cell/frame — the shared sink Modeling (§16) and UI (§21) import. A TYPING concern riding the existing import grammar (a new STDLIB_SURFACE partition + handle record schemas for record_check), NOT new grammar. Lands last: the .gen.fun seam and src/pickups.fun must typecheck handle expressions against it, and it carries the examples/assets golden pinning assert assets.coin_sfx == sound(coin_sfx). CRITICAL SCOPE: src/pickups.fun also references engine.render.Flip/Draw::Sprite and engine.audio.{Sound,Bus} — surface this epic does NOT own; the golden pins parse + the asset-specific assertion + the .gen.fun seam typecheck, and full src/pickups.fun end-to-end typecheck is gated on render/audio surface landing (a cross-epic edge, the driver's job). Rejected: (a) one mega story — not independently shippable; (b) folding the reader into importers — the hasher hashes OVER importer outputs and the manifest is the registry the emit/check seam consults, so reader-first is the dependency order; (c) folding strip into emission — strip is a release reference-graph walk needing emitted handle references, dev never strips, a downstream gate not an emission concern; (d) splitting surface from golden — the engine.assets partition is small (4 types + 5 fns) and the golden cannot typecheck the seam or assert without it." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHF, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:42:48.211Z", body: "probe" }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHE, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:41:59.627Z", body: "Decomposing the §17 .flvl levels-bake epic surfaced a cross-cutting prerequisite the epic description did NOT name: the funpack frontend is single-source end-to-end. resolve_imports(ast) runs over ONE Ast; the resolver only knows STDLIB_SURFACE (engine.*); stage_emit/run_test_pipeline/emit_tree_artifact all key on project.sources[0]; collect_sources walks only src/ (so levels/ and gen/ are uncollected). The arena example fundamentally requires multi-module USER-module resolution — arena_game.fun imports BOTH arena_world (schema) and arena (the generated seam), and any import arena_world.{Player} currently returns .Unknown_Module. This is in-epic foundational work, NOT a discovery against another epic: shared-bake-infra provides the bake-step/emitter/golden-harness/seam-import-path, but says nothing about user-module-to-user-module import resolution, which is squarely funpack-team frontend scope. The levels epic decomposes as a fixed seam chain: (1) multi-module user resolution [unnamed prerequisite, provable on arena .fun sources alone, no .flvl] -> (2) engine.world.Ref + engine.nav surface admission [pure surface.odin table growth, §08 Ref + Nav/Path/NavError, precondition for arena_world/arena_game typing] -> (3) .flvl lexer+parser [new flat-text grammar, parse-only, hand-built fixtures] -> (4) .flvl resolver+bake gates [names->typed Ref[T], prefab/loop expansion, fixed-point anchor resolution, all §17.4 gates as compile errors] -> (5) golden byte-match arena.gen.fun + arena green E2E [cross-epic leaf consuming shared-bake-infra]. The .flvl grammar is a THIRD config-class grammar distinct from .fun and the .fcfg smaller-config grammar (lex_fcfg): it has expressions (offset arithmetic, for-range bounds) .fcfg lacks, so it needs its own lexer/parser pair, mirroring why .fcfg did not reuse the .fun lexer. Rejected: folding multi-module resolution into the bake story (independently shippable on existing arena .fun files; gating it behind .flvl would hide the single-source break); folding Ref/nav surface into the parser story (surface growth is a self-contained table edit; the .flvl parser needs no schema); splitting lexer from parser (the grammar is small, lexer's only consumer is its parser); a discovery on multi-module resolution (in funpack scope, not external)." }
    - { id: 01KV29P5J4SRBBVM4ZSTP9RYHD, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:41:15.771Z", body: "Shared bake infrastructure (epic shared-bake-infrastructure-bui-mq2p3fbi, milestone bake-pipeline-seams) decomposes along four mechanism seams with this acyclic build order. (1) builds.fcfg + capability-derivation reader extends funpack/project.odin — adds a builds.fcfg parser production over the existing lex_fcfg/Cfg_Parser AND the §14.4 derived-capability tree-walk (a non-empty levels/ models/ ui/ assets/ subsystem dir => capability ON => its committed gen/*.gen.fun is expected). Pure config+tree reading; root, no in-epic deps. (2) gen/*.gen.fun canonical emitter is a NEW file producing the @doc-headed, formatter-canonical, diffable byte shape the committed goldens use (exemplar funpack-spec/examples/arena/gen/arena.gen.fun: file-leading @doc, import engine.<schema>.{…} + import <schema_module>.{…}, @doc-headed data/extern fn decls). Takes an in-memory seam model -> bytes; root, no in-epic deps; byte-shape TARGET is the committed exemplar. Distinct from emit.odin (binary artifact): this is source-text emission, the byte contract every downstream pipeline emitter reuses. (3) golden seam-comparison harness diffs freshly-emitted seam bytes against the committed gen/*.gen.fun, mirroring golden_emit_test.odin byte-equality + report_first_byte_diff and the resolve_spec_dir env-override/SKIP-warn resolution; a stale committed seam is a build error. Blocked by BOTH (1) and (2). (4) seam-import path through read_project: a baked gen/ seam joins the source set, derives its §15 module, rides lex->parse->gates->typecheck->contracts->flatten->evaluate like any .fun, honoring §17 schema/seam/behavior acyclic layering (a .gen.fun importing a behavior module is a compile error). Blocked by (1) only; consumes committed bytes as-is, independent of emitter/harness. Rejected: one mega 'bake infrastructure' story (the four mechanisms have distinct seams and a real dep-order); folding the emitter into the harness (the emitter is a reusable byte contract for all four authoring-source pipelines, independently shippable and double-emit-deterministic on its own). No new engine.* stdlib module — compiler-side plumbing only; honors spec §29 purity (no clock/host nondeterminism in funpack/**)." }
---

# Team: funpack

## Charter

Owns the funpack compiler — grammar, frontend, semantics, artifact emission, Index Contract

## Type

- Interaction archetype: stream_aligned
- Lifetime: persistent
- Terminates on milestone: <!-- none -->
- Status: active

## Scope

- Read globs: **
- Write globs: funpack/**

## Roster

- tech_lead: ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8
- engineer: ct-semantics-seat-c17dfd55-65eb-42c6-8b53-81404a81d88e
- implementer: ct-frontend-seat-c5a96c18-734a-4c61-883d-85ed54027522

## Interface

- Accepts: <!-- none -->
- Exposes: index-contract=funpack-spec: Index Contract — schema-versioned, exact-match NDJSON

## Lore

- Entries: 13 (13 live)
- [01KVXX1QYS5ATDF53BT8NMHEM2] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: let TUPLE-DESTRUCTURE is now legal (ADR 2026-06-24-let-tuple-destructure-binding): let (a, b, ...) = expr binds each name to a return-position tuple element — the threaded-Rng consume site let (value, next) = draw(rng). This was a frontend regression AGAINST spec doctrine, not a doctrine gap: spec 02 section 8 ALREADY named "the let/match that destructures such a [return-position tuple]" as canonical; only the grammar/fun.ebnf LetStmt production + the section-6 cheatsheet had failed to carry the let half. THE GRAMMAR SEAM (LL(1) preserved): a ( immediately after let is the one-token signal selecting parse_let_tuple over the single-name form, no backtrack — the former Let_Tuple_Destructure PARSE-error arm flipped from reject to parse a binder list. The binder list is FLAT plain lower-idents (snake_case), NOT the nested match-pattern grammar — structural matching stays in match; a refutable pattern in an irrefutable let binding was rejected (reintroduces exhaustiveness). TYPECHECK: bind_let_tuple zips each binder against the RHS ^Tuple_Type element; a count/non-tuple mismatch is the new closed-enum Type_Error Let_Tuple_Arity_Mismatch (named at the destructure seam, not a generic Type_Mismatch). LANDMINE worth the lore: bind into scope BY POINTER (^Scope), never a by-value map copy — an Odin map grown by-value reallocates only the LOCAL header, so a by-value bind flaked a later-statement Unresolved_Name (non-deterministic, only when the scope map happened to resize). The deliberate spec lives in the threaded-Rng determinism junction (rand_golden_test.odin), proving let-threaded draws equal the one-deep match-form seed-42 goldens. THE DEFERRED CROSS-TEAM SEAM (tracked: v19-artifact-reshape-carry-let-mqsnxil9): the v18 wire format encodes a let as ONE name scalar; the N-name binder list needs a v19 funpack<->runtime reshape (exact-match schema, runtime owns the decoder/interp half). Until then a tuple-let in a GAMEPLAY body (fn/behavior/query) is refused before emission (let_tuple_wire_gate -> Build_Error.Let_Tuple_Unsupported_Wire, exit 2, actionable message) rather than emitting an undecodable node — honoring the purity boundary AND avoiding the dual-interpreter parity trap. The form is fully usable+tested in test blocks today (off the wire, via the compiler evaluator). Rejected alternative: keep it banned and only fix the skills — leaves every threaded engine.rand call site needing a data-wrapper shim (the report workaround) and the structural trap intact.
- [01KV29P5J4SRBBVM4ZSTP9RYHY] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: Diagnostics are now a FIRST-CLASS COMPILER SEAM (the README fix-criteria quality-gate non-negotiable: a refusal is a rustc/gofmt block naming the offender, its file:line:col, the closed-enum rule, and a concise fix sentence — so an agent's write->check->fix loop converges). Three-layer information-loss fixed, source -> CLI.

THE SEAM (funpack/diagnostics.odin): the core Diagnostic{stage,rule,line,col,declaration,message,path} record; the PURE render_diagnostic(d, source) (a function of (Diagnostic, source) ALONE, so a golden pins it byte-for-byte: header path:line:col: rule (decl): message, then gutter-numbered excerpt + caret under col; line==0 collapses to header-only, col==0 omits the caret, an out-of-range line fails OPEN with an empty excerpt never a crash); and the FIVE per-stage mapping procs — parse_diagnostic / gate_diagnostic / type_diagnostic / contract_diagnostic / flatten_diagnostic — each a CLOSED switch over its stage error enum mapping arm -> (rule = the arm's own name (the machine-stable identifier, MUST equal the enum arm), message = the fix sentence). THOSE FIVE PROCS ARE THE SINGLE SOURCE OF TRUTH FOR HUMAN WORDING. A new enum arm is a visible compile gap in its proc (total switch), never a silently-wordless diagnostic.

PROVENANCE SOURCES (where line/col come from):
- expr_span(e: Expr) -> (line, col) in expr.odin: the TOTAL switch over the Expr union, leftmost-byte-of-the-construct anchor convention (literals on lead token; Binary_Expr on the LEAD OPERAND not the operator; Unary_Expr on the operator; Call/Member/With on callee/receiver/base; a parenthesized group passes its inner span through; only a tuple anchors on lparen). Synthetic downstream-built Expr nodes keep (0,0) = no-source-position, correct. Token carries col (1-based first byte in line) + offset (0-based byte in source), stamped by stage_lex tracking line_start.
- decl .line (1-based source line of the lead keyword, artifact-format §9 span provenance) on Fn/Behavior/Query/Test/Data/Enum/Signal/Let nodes: the decl-line anchor for offenders that own no expression span (gate offenders, contract behaviors, flatten signals, membership-class type faults).

THE INFORMATION-LOSS, FIXED AT THREE LAYERS:
1. STAGE SEAM — each stage keeps its coarse closed enum as the MACHINE CONTRACT and ADDS offender coordinates beside the arm: Parse gained stage_parse_located -> Parse_Verdict{err,line,col} via parser_stop_span (the token at clamp(p.pos,last) — the single-token-lookahead anchor); Gate's Gate_Unit/Gate_Verdict gained .line (decl line); Typecheck gained stage_typecheck_located -> Type_Verdict{err,line,col,declaration} via a pointer-threaded Type_Diag_Site sink on Check_Ctx.diag (expr_check stamps the INNERMOST offending expr span FIRST-WRITE-WINS; the body/test sweeps stamp the declaration via stamp_decl; statement-level rejects call stamp_expr); Contract/Flatten resolve the offender decl line from ast.behaviors/ast.signals.
2. PIPELINE DRIVER — run_module_pipeline_diag(source,index,modules,module) returns (report, Pipeline_Error, Diagnostic): each stage seam maps (arm,line,col,declaration) through its per-stage proc into ONE Diagnostic with path="". run_module_pipeline_named is its (report,err) projection so existing tests are unchanged. Project_Report.diagnostic carries it with path=failed_path; Build_Verdict.diagnostic threads the inner diagnostic on Compile_Failed ONLY (stage_build recovers the first-failing-module Diagnostic via run_project_pipeline — emit/index only know a coarse floor tripped).
3. CLI — eprint_module_diagnostic (test verb) + eprint_build_refusal (build/check) re-read diag.path and render; non-Compile_Failed Build_Error arms keep build_refusal_message.

THE MACHINE-CONTRACT-UNCHANGED INVARIANT (load-bearing): exit codes stay BYTE-IDENTICAL — compile error 2, failed assertions 1, all-pass 0; a compile error is NEVER a counted failure. The Diagnostic is the ADDED HUMAN BODY beside the coarse Pipeline_Error/Build_Error class, never a replacement. Adding/widening a Diagnostic is NOT an Index-Contract reshape (the Index Contract NDJSON is untouched) and NOT a schema bump.

PINNED GOLDENS (funpack/diagnostics_test.odin): the rendered block is pinned BYTE-FOR-BYTE for one representative arm of EACH stage through run_module_pipeline_diag (Parse=Wrong_Case, Gate=Arity_Exceeded, Typecheck=Type_Mismatch, Contract=Render_Emits, Closure=Unclosed_Signal) plus the membership decl-line pin. Each source is a small self-contained snippet tripping exactly its stage's FIRST offender (first-offender determinism), so identical source => identical bytes. These goldens stop diagnostic quality from silently regressing: a wording edit, a span-anchor shift, or a gutter/caret-shape change moves the bytes and fails.

TWO PRECISION GAPS, RESOLVED. (a) Wrong_Case caret off-by-one DEFERRED (task tighten-parser-reject-span-wro-mqdbq3w8): parser_stop_span anchors at p.pos, which a post-advance casing reject has already moved past the offending identifier, so the caret lands one token late (col 14 the '{' not col 7 'widget' for 'thing widget {'). NOT a one-line fix — it ramifies across 32 Wrong_Case sites + the post-advance pattern across ~203 or_return sites; the correct fix is a parser-wide reject-span discipline, its own bounded change. The col-14 behavior is LOCKED in test_diag_pin_parse_wrong_case with a NOTE; the task's acceptance flips that golden. (b) Membership-class type checks (layer registry / @migrate / index paths / expose closure) FIXED INLINE: they own no expression span so they rendered header-only at line 0; threading the Type_Diag_Site sink into check_layer_registry/check_migrations/check_index_paths/check_expose_closure and calling stamp_decl(site, decl.name, decl.line) at each reject now anchors them on the owning declaration's line (col 0) — the SAME gate-offender decl-line shape (Expose_Closure_Verdict gained a .line field). Pinned in test_diag_pin_membership_expose_closure_anchors_decl_line.

WORDING/SPEC-CITE REVIEW (every message cross-checked against its enum arm's rich doc-comment AND ../funpack-spec/spec): one fix — Wrong_Case cite tightened §02 -> §02 §1 (the casing rules live in §02 §1 Lexis 'Identifiers & casing', NOT §02 §4 Names/calling-convention). All other cites verified accurate against the spec section headers, including the non-obvious ones: Index_Unknown_Thing/Index_Unknown_Field/Malformed_Index_Path -> §05 §3 (the FieldPath-head rule + the error name are anchored there); Malformed_String_Escape -> lexical-core §4 (the closed escape set \" \{ \}); Reserved_Signal_Name -> §11 §4 (Trigger/Contact engine-routed); Unregistered_Layer -> §11 §5; the @migrate family -> §05 §6; the probe family -> §28 §4 On-table; Duplicate_Declaration + the four budget gates -> §01 P5 (which covers BOTH structural budgets AND the duplication ban).

FUTURE SEATS: diagnostics are now a seam you EXTEND, not improvise. Add an enum arm -> add its mapping-proc message (the total switch forces it) with an accurate spec cite (rule string = the arm name). Add offender coordinates beside the arm (expr_span for an expression fault, decl .line for a declaration fault). The five mapping procs are the wording's single source of truth — never word a refusal anywhere else. Validate: build/lint/test all green (funpack 1257, runtime 533); the byte pins are the regression floor.
- [01KV29P5J4SRBBVM4ZSTP9RYHV] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: ARTIFACT_SCHEMA_VERSION v16→17 — the §19 textured-render cross-boundary links (ADR 2026-06-12-v17-textured-render-cross-boundary-links). v16 carried the sprite PIXELS but no bridge from a sprite/tile REFERENCE to them, so every real dungeon sprite/tile fail-closed to no texture (runtime Stage-2b). ONE 16→17 bump (window CLOSED at v16 per lore #20 — committed runtime/testdata pins v16) bundles THREE coupled changes; three separate bumps would each force a full testdata reconcile. (1) [assets] atlas keyed by the manifest HANDLE name — bake_atlas_assets keys Baked_Atlas.name by entry.name (dungeon_atlas, the [name] block), NOT parsed.name (DungeonAtlas, the .atlas-declared name no Draw_Sprite references): 'atlas dungeon_atlas HASH 8'. Keying by the handle name makes the manifest bake and the assets bake agree by construction (the same name the AtlasHandle const carries), so asset_region(handle,cell) resolves with no per-sprite rewrite. (2) THE ASSET-HANDLE-RESOLUTION-VIA-CONST-CARRY SEAM (emit_whole_module_lower.odin): the entrypoint reaches its handle const through a WHOLE-MODULE import (import assets, then assets.dungeon_atlas) which binds no bare name — distinct from the brace-group level-seam import (import dungeon.{terrain}) the v6/v15 carry handles. ROOT CAUSE of the silent drop CONFIRMED: the assets gen module IS collected (assets capability ON via assets/assets.manifest; the whole gen/ dir is walked for *.gen.fun, so gen/assets.gen.fun reaches sources AND build_sibling_module_asts) — the drop was NOT a collection failure, it was that the seam-fn carry (imported_user_module, emit_seam_fns.odin) REQUIRES brace-group members and import assets is memberless. The fix is the whole-module carry path. TWO mechanisms, mirroring v6/v15: CARRY — collect_whole_module_const_records carries each whole-module-referenced handle const into [functions] as a 'function NAME const' record (the v15 imported-const form, the seam module's span) with the §26 typed handle value READ from the seam AST's declared let type (AtlasHandle/SoundHandle/MeshHandle/TextureHandle — NEVER hardcoded to atlas). LOWER — lower_whole_module_refs rewrites every module.NAME member-expr in the entrypoint AST's behavior/fn/let bodies to a bare Name_Expr{NAME} (the v6 qualified→bare lowering, in-place on the fresh-per-build entrypoint AST — emit is terminal, emit_tree_artifact re-parses per call so double-build determinism holds), so the runtime resolves it by bare name with NO special-case (the operator's clean-path choice over a runtime stopgap). COLLISION GUARD (the v6 disambiguation): a whole-module const lowers to its bare name only when no own-module fn/let binds that name; a collision is Emit_Error.Whole_Module_Collision → Compile_Failed (exit 2), never two [functions] records under one bare name. Wired in stage_emit_indexed: collect const records BEFORE lowering (the carry reads the qualified refs the lowering then strips), then lower, then concat after the brace-group carries. (3) [tilemaps] per-layer atlas + per-tile cell: Tileset_Asset.atlas→Project_Tile.atlas→Baked_Tile_Layer.atlas (per-layer) and Tileset_Tile.cell_x/y→Baked_Tile.cell_x/y (per-tile). Lead line 'tilemap NAME CELL_SIZE COLS ROWS ANCHOR_X ANCHOR_Y ATLAS PALETTE_COUNT' (- for a degenerate palette-less layer); palette 'tile NAME SOLID CELL_X CELL_Y'. The runtime resolves a tile's texture through asset_region(ATLAS,(CELL_X,CELL_Y)) exactly as a sprite does. A layer whose palette mixes tilesets with DIFFERENT atlases is the Tileset_Atlas_Conflict bake gate (one atlas per layer) — refused, never silently picking one (the friction-surfaced contradiction; no committed example mixes atlases, dungeon/warren are single-tileset). NO new section, NO new sub-record KEYWORD — the atlas lead keyword and the tile/row sub-record keywords already exist; only their token shapes and the atlas record's NAME token change, plus a widened [functions] population. The 4 cross-pin tests SKIP-loudly (committed runtime copy v16 < emitter v17) — the BORN-STAGED hatch; the runtime reconcile (Stage 4) regens them to v17. Pong (asset-less/level-less) regens by the version stamp alone. funpack green (1193 tests).
- [01KV29P5J4SRBBVM4ZSTP9RYHR] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: The §19 [assets] artifact section (v16 bump, ADR 2026-06-12-assets-section-base64-content-addressed) carries the decoded sprite pixels the textured Draw_Sprite{atlas,cell} needs — the FIRST artifact section whose payload is binary data, not a checked-AST projection. THREE grammar decisions. (1) base64 pixel encoding (core:encoding/base64, std RFC-4648): the canonical RGBA8 buffer (W·H·4 bytes, row-major, import_image's .alpha_add_if_missing output) -> one ASCII b64:<…> token on one line, so the committed text golden stays diffable and the runtime decodes with the same core pkg. Rejected: raw length-prefixed binary (loses text-golden diffability, fights the §2.1 LF-framed lexical frame) and hex (2× bloat for the same diffability — base64 is ~4/3). (2) content-addressed dedup by image hash: one image-record per DISTINCT content hash, atlases reference it by IMAGE_HASH, so two atlases on one sheet hold the blob ONCE. The hash is the SAME §2 content address the manifest keys the image by, so the manifest bake and the assets bake agree by construction. (3) ATLAS-ROOTED image set + grid-lowered region rects: the [assets] images are exactly the ones an atlas references (built from the atlas walk, NOT standalone manifest image entries — an unsliced image is never blitted), and region (a NEW closed sub-record keyword) derives PX_X=cell.x·grid_w, PX_Y=cell.y·grid_h, PX_W/H=grid_w/h from the atlas grid clause — so (atlas-name,cell-name)->(image pixels,pixel rect) resolves from the artifact alone. THE BAKE->EMIT PIXEL-THREADING SEAM is the load-bearing mechanism: bake_resolve_image (the manifest bake, asset_bake.odin) FREES the decoded pixels because the manifest needs only the hash; bake_tree_assets/bake_resolve_image_pixels (asset_emit_bake.odin) is its pixel-PRESERVING twin that re-decodes off disk and threads a Baked_Assets model (Baked_Image/Baked_Atlas/Baked_Region) into a NEW Emit_Input.assets field through stage_emit_indexed. emit_tree_artifact bakes it only when asset_tree_has_manifest (already staleness-gated by stage_build); an asset-less game threads the empty Baked_Assets and writes the constant [assets 0] tail (the [nav 0]/[tilemaps 0] precedent). The v16 bump CLOSES the open window per Lore #20: four cross-package byte-pins (golden_tilemap_e2e dungeon+warren, golden_krognid, golden_statequery) exact-match runtime/testdata/*.artifact, all stamped v15. They DO NOT FAIL on the funpack-side bump because each has a built-in committed_version < ARTIFACT_SCHEMA_VERSION -> SKIP-loudly hatch (the BORN-STAGED seam) — so they SKIP until the Stage-2 runtime reconcile (bump runtime->16, parse [assets], regen the four testdata copies) lands. That reconcile is the runtime team's commit; it determines commit ordering for the next stage. Pong (asset-less) regens to the v16 stamp + [assets 0] tail; the dungeon (162B PNG, 64×32 RGBA8, 8 cells) is the N>0 proof (test_golden_dungeon_build_carries_populated_assets_section: [assets 2] = 1 deduped image + DungeonAtlas with 8 region rects, round-trips through parse_artifact, double-build byte-identical).
- [01KV29P5J4SRBBVM4ZSTP9RYHQ] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: ARTIFACT_SCHEMA_VERSION bump discipline — the open-window rule (ratified in ADR 2026-06-11-path-projection-rides-v15-open-window, the §8 Path projection case). The stamp pins the FORMAT (sections, record grammars, node kinds, value encodings), not the population a given source emits into those sections. Two consequences. (1) A population widening inside an already-ratified closed mechanism (the §8 synthesized engine-type [data] projections, the v6/v15 carry populations) may land under an OPEN version stamp — open meaning no committed artifact outside funpack/ pins bytes at that version (runtime/testdata byte seams staged/SKIPping, no shipped golden). Bumping anyway burns a version with zero discriminating power: every reader implements the new version identically to the old. (2) The window CLOSES the moment any cross-package byte pin lands at that version (Phase-B-style runtime reconcile, a committed testdata artifact): from then on ANY byte movement at that stamp — however precedented the mechanism — is contract drift and requires a bump. Bump when an existing reader's pinned bytes would move, never to annotate a change no reader can distinguish. Alternatives rejected for the Path case: v16 (no observable difference, doubles the staged-seam reconcile); runtime-side hardcoded field types (breaks the §8 design — the artifact carries the projection, the surface owns the type, the runtime stays schema-driven); deferring to land atomically with Phase B (ships a self-contradictory artifact whose [data] cannot type its own default tokens). This is the same boundary as the Index Contract's reshape rule: exact-match consumers define when a change is a reshape.
- [01KV29P5J4SRBBVM4ZSTP9RYHP] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: The §05 @stub typed-hole milestone (typed-holes-stub-release-ban) decomposes along the frontend's standing parse -> typecheck -> {index, fallback-eval, release-ban} -> fixture seam order, six independently-shippable stories. CRITICAL GRAMMAR DISTINCTION grounding the first cut: @stub is NOT a leading @doc/@gtag-style prefix directive — spec §05 §1 says it 'stands in body/expression position'. So the parse seam is NOT an extension of parse_directive's prefix loop (that handles @doc/@gtag accumulated into the Directives struct, parser.odin ~L408); it is a NEW BODY-position production — a holed fn/behavior replaces its Statement-sequence body (Fn_Node.body / Behavior_Node.step.body, parser.odin ~L164/L184) with the hole carrying the declared type T and an optional fallback Expr. That is why the AST-representation seam is the natural first story, distinct from directive-prefix grammar. (1) PARSE @stub(T)/@stub(T,fallback) into the body-bearing AST nodes — a new body-position production + the carry of the hole's declared T and optional fallback expr. Root, no in-milestone dep. (2) TYPECHECK a holed decl against its DECLARED return type so callers typecheck against the hole: the holed body short-circuits check_fn_body's statement-walk (typecheck.odin check_fn_body ~L277, expected_return = the declared -> R) — the hole's T must EQUAL the declared return; callers already typecheck against the intact signature via expr_check over Call_Expr, so caller-against-hole falls out once the signature stands and the body is the hole. Blocked by (1). (3) DERIVE the stub index field from the AST — kill the always-false: Decl_Record.stub is hardcoded false in body_less_decl/fn_decl_record/behavior_decl_record/test_decl_record (index_decl.odin) per the source_has_expose 'parser admits only @doc/@gtag' rationale, which (1) makes obsolete. NOT a schema reshape: stub is an EXISTING mandatory field flipping from constant-false to AST-derived, the field set is unchanged, so it does NOT bump INDEX_SCHEMA_VERSION. Blocked by (1). (4) @stub(T,fallback) EVALUATION — the approximation runs so the game stays playable: stage_evaluate_indexed runs fn/behavior bodies; a fallback hole evaluates its fallback Expr in the decl's env (evaluate.odin/value.odin). A typecheck-only @stub(T) with no fallback has nothing to run — dev-compiles, never shipped, a runtime reach is undefined. Blocked by (2) (the fallback must typecheck against T first). (5) --release HOLE-BAN compile error (P8 / §29 §4: you cannot ship a hole): NO --release flag exists today — main.odin dispatches build with no mode, stage_build takes no mode. This story adds the release-mode plumbing (a Build_Mode threaded build verb, mirroring asset_strip.odin's existing Strip_Mode{Dev,Release}) AND the gate: any holed decl under --release is exit-2 compile error (never a counted failure — build has no exit-1 tier). A pure-AST mode-flag verdict (§29 §1: 'pure AST or a mode flag'), so it stays on the pure side. Blocked by (1) — independent of (2)/(3)/(4). (6) GOVERNANCE FIXTURE exercising a typecheck-only @stub(T) hole + a @stub(T,fallback) hole end-to-end: authored in funpack-spec examples (NO @stub example exists there yet — grep-confirmed empty), driven through resolve_spec_dir env-override/SKIP-warn golden harness. Asserts: dev build compiles + the stub index field is true for both holes + the fallback runs + --release rejects exit 2. funpack does not grammar-include what it cannot run, so this leaf is the surface-done proof. Blocked by (3),(4),(5). OUT OF SCOPE (NOT planned): @todo expiry (deferred — open design decision) and the funpack warden query subcommands (separate milestone — this only populates the stub index field). Rejected: (a) one mega 'parse+type+index+ban' story — not independently shippable; (b) folding the release-ban into parse — the ban needs release-mode plumbing that does not exist and is a downstream mode-gate, not a grammar concern; (c) treating @stub as a parse_directive prefix arm like @doc/@gtag — WRONG per §05, it is a body/expr-position hole, the single most important grounding correction; (d) bumping INDEX_SCHEMA_VERSION for the stub-field derivation — the field already exists mandatory, only its derivation changes.
- [01KV29P5J4SRBBVM4ZSTP9RYHG] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: The §19 asset pipeline decomposes along the same source→importer→(content-hashed asset + .gen.fun seam) shape the frontend follows, split into five independently-shippable stories on the shared-bake-infrastructure substrate (bake-step decl, .gen.fun emitter byte contract, golden-comparison harness, seam-import path — assumed present, never re-planned). Seam order and why each cut is the natural boundary: (1) MANIFEST READER + CONTENT HASHER — the committed assets.manifest (name -> kind/source/importer-version/deps/hash/out blocks) is the source of truth for handle resolution and the closed name registry; hash = H(source bytes + importer version + dep hashes), §2. Reader+hasher is one seam: the hash rule is uniform across kinds and the manifest is the index every later seam consults. Ships behind a unit fixture over examples/assets/assets.manifest. (2) PER-FORMAT IMPORTERS (.fpm model, .atlas image+slice, raw audio) — one engine-closed deterministic importer per source kind (§4), each producing a content-hashed asset whose hash feeds the manifest; parallelizable DAG nodes (atlas deps-on its raw image hash). Distinct from the reader: the importers are the pure source→asset functions the hasher hashes OVER. (3) HANDLE-CONSTANT SEAM EMISSION + CLOSED-REGISTRY CHECK — emits assets.gen.fun byte-matching examples/assets/gen/assets.gen.fun (one typed handle constant per registered asset, lowercase folder-namespaced) via the shared .gen.fun emitter byte contract; the @gtag-for-assets P7 closed registry makes a name not in the manifest a COMPILE ERROR; string constructors mesh/atlas/sound checked against the manifest too (assets.coin_sfx == sound(coin_sfx)). Depends on the reader for the name set. (4) RELEASE DEAD-ASSET ELIMINATION + ASSET REPORT — the P5 dead-code gate applied to content (§5): release strips any asset no handle references and emits assets.report.txt byte-matching the example; a release-mode reference-graph walk over the emitted handle set, downstream of seam emission (needs handle references for reachability); dev mode bakes the dirty subgraph and never strips. (5) engine.assets SURFACE ADMISSION + GOLDEN INTEGRATION — admit engine.assets to funpack/surface.odin with EXACTLY the §26 line-78 names: types MeshHandle/TextureHandle/SoundHandle/AtlasHandle, fns mesh/texture/sound/atlas/cell/frame — the shared sink Modeling (§16) and UI (§21) import. A TYPING concern riding the existing import grammar (a new STDLIB_SURFACE partition + handle record schemas for record_check), NOT new grammar. Lands last: the .gen.fun seam and src/pickups.fun must typecheck handle expressions against it, and it carries the examples/assets golden pinning assert assets.coin_sfx == sound(coin_sfx). CRITICAL SCOPE: src/pickups.fun also references engine.render.Flip/Draw::Sprite and engine.audio.{Sound,Bus} — surface this epic does NOT own; the golden pins parse + the asset-specific assertion + the .gen.fun seam typecheck, and full src/pickups.fun end-to-end typecheck is gated on render/audio surface landing (a cross-epic edge, the driver's job). Rejected: (a) one mega story — not independently shippable; (b) folding the reader into importers — the hasher hashes OVER importer outputs and the manifest is the registry the emit/check seam consults, so reader-first is the dependency order; (c) folding strip into emission — strip is a release reference-graph walk needing emitted handle references, dev never strips, a downstream gate not an emission concern; (d) splitting surface from golden — the engine.assets partition is small (4 types + 5 fns) and the golden cannot typecheck the seam or assert without it.
- [01KV29P5J4SRBBVM4ZSTP9RYHF] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: probe
- [01KV29P5J4SRBBVM4ZSTP9RYHE] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: Decomposing the §17 .flvl levels-bake epic surfaced a cross-cutting prerequisite the epic description did NOT name: the funpack frontend is single-source end-to-end. resolve_imports(ast) runs over ONE Ast; the resolver only knows STDLIB_SURFACE (engine.*); stage_emit/run_test_pipeline/emit_tree_artifact all key on project.sources[0]; collect_sources walks only src/ (so levels/ and gen/ are uncollected). The arena example fundamentally requires multi-module USER-module resolution — arena_game.fun imports BOTH arena_world (schema) and arena (the generated seam), and any import arena_world.{Player} currently returns .Unknown_Module. This is in-epic foundational work, NOT a discovery against another epic: shared-bake-infra provides the bake-step/emitter/golden-harness/seam-import-path, but says nothing about user-module-to-user-module import resolution, which is squarely funpack-team frontend scope. The levels epic decomposes as a fixed seam chain: (1) multi-module user resolution [unnamed prerequisite, provable on arena .fun sources alone, no .flvl] -> (2) engine.world.Ref + engine.nav surface admission [pure surface.odin table growth, §08 Ref + Nav/Path/NavError, precondition for arena_world/arena_game typing] -> (3) .flvl lexer+parser [new flat-text grammar, parse-only, hand-built fixtures] -> (4) .flvl resolver+bake gates [names->typed Ref[T], prefab/loop expansion, fixed-point anchor resolution, all §17.4 gates as compile errors] -> (5) golden byte-match arena.gen.fun + arena green E2E [cross-epic leaf consuming shared-bake-infra]. The .flvl grammar is a THIRD config-class grammar distinct from .fun and the .fcfg smaller-config grammar (lex_fcfg): it has expressions (offset arithmetic, for-range bounds) .fcfg lacks, so it needs its own lexer/parser pair, mirroring why .fcfg did not reuse the .fun lexer. Rejected: folding multi-module resolution into the bake story (independently shippable on existing arena .fun files; gating it behind .flvl would hide the single-source break); folding Ref/nav surface into the parser story (surface growth is a self-contained table edit; the .flvl parser needs no schema); splitting lexer from parser (the grammar is small, lexer's only consumer is its parser); a discovery on multi-module resolution (in funpack scope, not external).
- [01KV29P5J4SRBBVM4ZSTP9RYHD] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: Shared bake infrastructure (epic shared-bake-infrastructure-bui-mq2p3fbi, milestone bake-pipeline-seams) decomposes along four mechanism seams with this acyclic build order. (1) builds.fcfg + capability-derivation reader extends funpack/project.odin — adds a builds.fcfg parser production over the existing lex_fcfg/Cfg_Parser AND the §14.4 derived-capability tree-walk (a non-empty levels/ models/ ui/ assets/ subsystem dir => capability ON => its committed gen/*.gen.fun is expected). Pure config+tree reading; root, no in-epic deps. (2) gen/*.gen.fun canonical emitter is a NEW file producing the @doc-headed, formatter-canonical, diffable byte shape the committed goldens use (exemplar funpack-spec/examples/arena/gen/arena.gen.fun: file-leading @doc, import engine.<schema>.{…} + import <schema_module>.{…}, @doc-headed data/extern fn decls). Takes an in-memory seam model -> bytes; root, no in-epic deps; byte-shape TARGET is the committed exemplar. Distinct from emit.odin (binary artifact): this is source-text emission, the byte contract every downstream pipeline emitter reuses. (3) golden seam-comparison harness diffs freshly-emitted seam bytes against the committed gen/*.gen.fun, mirroring golden_emit_test.odin byte-equality + report_first_byte_diff and the resolve_spec_dir env-override/SKIP-warn resolution; a stale committed seam is a build error. Blocked by BOTH (1) and (2). (4) seam-import path through read_project: a baked gen/ seam joins the source set, derives its §15 module, rides lex->parse->gates->typecheck->contracts->flatten->evaluate like any .fun, honoring §17 schema/seam/behavior acyclic layering (a .gen.fun importing a behavior module is a compile error). Blocked by (1) only; consumes committed bytes as-is, independent of emitter/harness. Rejected: one mega 'bake infrastructure' story (the four mechanisms have distinct seams and a real dep-order); folding the emitter into the harness (the emitter is a reusable byte contract for all four authoring-source pipelines, independently shippable and double-emit-deterministic on its own). No new engine.* stdlib module — compiler-side plumbing only; honors spec §29 purity (no clock/host nondeterminism in funpack/**).
