---
schema_version: 28
team:
  slug: funpack
  team_type: stream_aligned
  charter: "Owns the funpack compiler — grammar, frontend, semantics, artifact emission, Index Contract"
  lifetime: persistent
  terminates_on_milestone: null
  status: active
  created_at: 2026-06-05T02:40:25.168Z
scope:
  read: ["**"]
  write: ["funpack/**"]
roster:
  tech_lead: ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8
  engineer: ct-semantics-seat-c17dfd55-65eb-42c6-8b53-81404a81d88e
  implementer: ct-frontend-seat-c5a96c18-734a-4c61-883d-85ed54027522
interface:
  accepts: []
  exposes:
    - { name: "index-contract", schema_ref: "funpack-spec: Index Contract — schema-versioned, exact-match NDJSON" }
lore:
  count: 7
  live: 4
  recent:
    - { id: 13, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:43:08.618Z", body: "The §19 asset pipeline decomposes along the same source→importer→(content-hashed asset + .gen.fun seam) shape the frontend follows, split into five independently-shippable stories on the shared-bake-infrastructure substrate (bake-step decl, .gen.fun emitter byte contract, golden-comparison harness, seam-import path — assumed present, never re-planned). Seam order and why each cut is the natural boundary: (1) MANIFEST READER + CONTENT HASHER — the committed assets.manifest (name -> kind/source/importer-version/deps/hash/out blocks) is the source of truth for handle resolution and the closed name registry; hash = H(source bytes + importer version + dep hashes), §2. Reader+hasher is one seam: the hash rule is uniform across kinds and the manifest is the index every later seam consults. Ships behind a unit fixture over examples/assets/assets.manifest. (2) PER-FORMAT IMPORTERS (.fpm model, .atlas image+slice, raw audio) — one engine-closed deterministic importer per source kind (§4), each producing a content-hashed asset whose hash feeds the manifest; parallelizable DAG nodes (atlas deps-on its raw image hash). Distinct from the reader: the importers are the pure source→asset functions the hasher hashes OVER. (3) HANDLE-CONSTANT SEAM EMISSION + CLOSED-REGISTRY CHECK — emits assets.gen.fun byte-matching examples/assets/gen/assets.gen.fun (one typed handle constant per registered asset, lowercase folder-namespaced) via the shared .gen.fun emitter byte contract; the @gtag-for-assets P7 closed registry makes a name not in the manifest a COMPILE ERROR; string constructors mesh/atlas/sound checked against the manifest too (assets.coin_sfx == sound(coin_sfx)). Depends on the reader for the name set. (4) RELEASE DEAD-ASSET ELIMINATION + ASSET REPORT — the P5 dead-code gate applied to content (§5): release strips any asset no handle references and emits assets.report.txt byte-matching the example; a release-mode reference-graph walk over the emitted handle set, downstream of seam emission (needs handle references for reachability); dev mode bakes the dirty subgraph and never strips. (5) engine.assets SURFACE ADMISSION + GOLDEN INTEGRATION — admit engine.assets to funpack/surface.odin with EXACTLY the §26 line-78 names: types MeshHandle/TextureHandle/SoundHandle/AtlasHandle, fns mesh/texture/sound/atlas/cell/frame — the shared sink Modeling (§16) and UI (§21) import. A TYPING concern riding the existing import grammar (a new STDLIB_SURFACE partition + handle record schemas for record_check), NOT new grammar. Lands last: the .gen.fun seam and src/pickups.fun must typecheck handle expressions against it, and it carries the examples/assets golden pinning assert assets.coin_sfx == sound(coin_sfx). CRITICAL SCOPE: src/pickups.fun also references engine.render.Flip/Draw::Sprite and engine.audio.{Sound,Bus} — surface this epic does NOT own; the golden pins parse + the asset-specific assertion + the .gen.fun seam typecheck, and full src/pickups.fun end-to-end typecheck is gated on render/audio surface landing (a cross-epic edge, the driver's job). Rejected: (a) one mega story — not independently shippable; (b) folding the reader into importers — the hasher hashes OVER importer outputs and the manifest is the registry the emit/check seam consults, so reader-first is the dependency order; (c) folding strip into emission — strip is a release reference-graph walk needing emitted handle references, dev never strips, a downstream gate not an emission concern; (d) splitting surface from golden — the engine.assets partition is small (4 types + 5 fns) and the golden cannot typecheck the seam or assert without it." }
    - { id: 11, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:41:59.627Z", body: "Decomposing the §17 .flvl levels-bake epic surfaced a cross-cutting prerequisite the epic description did NOT name: the funpack frontend is single-source end-to-end. resolve_imports(ast) runs over ONE Ast; the resolver only knows STDLIB_SURFACE (engine.*); stage_emit/run_test_pipeline/emit_tree_artifact all key on project.sources[0]; collect_sources walks only src/ (so levels/ and gen/ are uncollected). The arena example fundamentally requires multi-module USER-module resolution — arena_game.fun imports BOTH arena_world (schema) and arena (the generated seam), and any import arena_world.{Player} currently returns .Unknown_Module. This is in-epic foundational work, NOT a discovery against another epic: shared-bake-infra provides the bake-step/emitter/golden-harness/seam-import-path, but says nothing about user-module-to-user-module import resolution, which is squarely funpack-team frontend scope. The levels epic decomposes as a fixed seam chain: (1) multi-module user resolution [unnamed prerequisite, provable on arena .fun sources alone, no .flvl] -> (2) engine.world.Ref + engine.nav surface admission [pure surface.odin table growth, §08 Ref + Nav/Path/NavError, precondition for arena_world/arena_game typing] -> (3) .flvl lexer+parser [new flat-text grammar, parse-only, hand-built fixtures] -> (4) .flvl resolver+bake gates [names->typed Ref[T], prefab/loop expansion, fixed-point anchor resolution, all §17.4 gates as compile errors] -> (5) golden byte-match arena.gen.fun + arena green E2E [cross-epic leaf consuming shared-bake-infra]. The .flvl grammar is a THIRD config-class grammar distinct from .fun and the .fcfg smaller-config grammar (lex_fcfg): it has expressions (offset arithmetic, for-range bounds) .fcfg lacks, so it needs its own lexer/parser pair, mirroring why .fcfg did not reuse the .fun lexer. Rejected: folding multi-module resolution into the bake story (independently shippable on existing arena .fun files; gating it behind .flvl would hide the single-source break); folding Ref/nav surface into the parser story (surface growth is a self-contained table edit; the .flvl parser needs no schema); splitting lexer from parser (the grammar is small, lexer's only consumer is its parser); a discovery on multi-module resolution (in funpack scope, not external)." }
    - { id: 10, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-06T18:41:15.771Z", body: "Shared bake infrastructure (epic shared-bake-infrastructure-bui-mq2p3fbi, milestone bake-pipeline-seams) decomposes along four mechanism seams with this acyclic build order. (1) builds.fcfg + capability-derivation reader extends funpack/project.odin — adds a builds.fcfg parser production over the existing lex_fcfg/Cfg_Parser AND the §14.4 derived-capability tree-walk (a non-empty levels/ models/ ui/ assets/ subsystem dir => capability ON => its committed gen/*.gen.fun is expected). Pure config+tree reading; root, no in-epic deps. (2) gen/*.gen.fun canonical emitter is a NEW file producing the @doc-headed, formatter-canonical, diffable byte shape the committed goldens use (exemplar funpack-spec/examples/arena/gen/arena.gen.fun: file-leading @doc, import engine.<schema>.{…} + import <schema_module>.{…}, @doc-headed data/extern fn decls). Takes an in-memory seam model -> bytes; root, no in-epic deps; byte-shape TARGET is the committed exemplar. Distinct from emit.odin (binary artifact): this is source-text emission, the byte contract every downstream pipeline emitter reuses. (3) golden seam-comparison harness diffs freshly-emitted seam bytes against the committed gen/*.gen.fun, mirroring golden_emit_test.odin byte-equality + report_first_byte_diff and the resolve_spec_dir env-override/SKIP-warn resolution; a stale committed seam is a build error. Blocked by BOTH (1) and (2). (4) seam-import path through read_project: a baked gen/ seam joins the source set, derives its §15 module, rides lex->parse->gates->typecheck->contracts->flatten->evaluate like any .fun, honoring §17 schema/seam/behavior acyclic layering (a .gen.fun importing a behavior module is a compile error). Blocked by (1) only; consumes committed bytes as-is, independent of emitter/harness. Rejected: one mega 'bake infrastructure' story (the four mechanisms have distinct seams and a real dep-order); folding the emitter into the harness (the emitter is a reusable byte contract for all four authoring-source pipelines, independently shippable and double-emit-deterministic on its own). No new engine.* stdlib module — compiler-side plumbing only; honors spec §29 purity (no clock/host nondeterminism in funpack/**)." }
    - { id: 6, author: "ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8", created_at: "2026-06-05T21:26:26.676Z", body: "The funpack frontend decomposes along a fixed seam order — lexer/parser -> resolve/type -> contract/gate -> golden integration — and each seam is split so it is independently shippable behind the existing test harness; the parser-vs-typer boundary is the natural story cut. Two pong-surface checks are DISTINCT stages, never one: the per-behavior NODE check (a behavior is well-formed for its slot — Update/Render/Startup, spec §06) and the cross-behavior EDGE check (effect closure — every emitted signal has a downstream consuming stage, §04/§07). The edge check requires the depth-first flattened pipeline as the total order, so pipeline flattening is a prerequisite seam for effect closure and lands together with it, downstream of the node check. Read-side surface (§08 View[T]/first/fold) and bindings/setup (§23 bindings()/setup()) are TYPING concerns that ride the existing expression grammar (calls, members, lambdas, lists) once surface.odin admits the new engine.* modules — they are not new grammar. Typing a behavior body (blackboard reads-as-params/writes-as-return §06 §3; closed command/signal return types §04 §1 — Spawn/Draw/[Goal]; Input/Time resource params) is the node check's precondition, kept separate from the node check itself. Rejected: one mega 'parse + type + check pong' story (not independently shippable, and the parser/typer seam is the existing frontend boundary); folding node-check and edge-check into one 'contracts' story (the spec separates them, the edge check needs the flattened pipeline the node check does not, and a node-check-only story ships slot well-formedness on its own). Consolidates lore #1." }
---

# Team: funpack

## Charter

Owns the funpack compiler — grammar, frontend, semantics, artifact emission, Index Contract

## Type

- Interaction archetype: stream_aligned
- Lifetime: persistent
- Terminates on milestone: <!-- none -->
- Status: active

## Scope

- Read globs: **
- Write globs: funpack/**

## Roster

- tech_lead: ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8
- engineer: ct-semantics-seat-c17dfd55-65eb-42c6-8b53-81404a81d88e
- implementer: ct-frontend-seat-c5a96c18-734a-4c61-883d-85ed54027522

## Interface

- Accepts: <!-- none -->
- Exposes: index-contract=funpack-spec: Index Contract — schema-versioned, exact-match NDJSON

## Lore

- Entries: 7 (4 live)
- [13] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: The §19 asset pipeline decomposes along the same source→importer→(content-hashed asset + .gen.fun seam) shape the frontend follows, split into five independently-shippable stories on the shared-bake-infrastructure substrate (bake-step decl, .gen.fun emitter byte contract, golden-comparison harness, seam-import path — assumed present, never re-planned). Seam order and why each cut is the natural boundary: (1) MANIFEST READER + CONTENT HASHER — the committed assets.manifest (name -> kind/source/importer-version/deps/hash/out blocks) is the source of truth for handle resolution and the closed name registry; hash = H(source bytes + importer version + dep hashes), §2. Reader+hasher is one seam: the hash rule is uniform across kinds and the manifest is the index every later seam consults. Ships behind a unit fixture over examples/assets/assets.manifest. (2) PER-FORMAT IMPORTERS (.fpm model, .atlas image+slice, raw audio) — one engine-closed deterministic importer per source kind (§4), each producing a content-hashed asset whose hash feeds the manifest; parallelizable DAG nodes (atlas deps-on its raw image hash). Distinct from the reader: the importers are the pure source→asset functions the hasher hashes OVER. (3) HANDLE-CONSTANT SEAM EMISSION + CLOSED-REGISTRY CHECK — emits assets.gen.fun byte-matching examples/assets/gen/assets.gen.fun (one typed handle constant per registered asset, lowercase folder-namespaced) via the shared .gen.fun emitter byte contract; the @gtag-for-assets P7 closed registry makes a name not in the manifest a COMPILE ERROR; string constructors mesh/atlas/sound checked against the manifest too (assets.coin_sfx == sound(coin_sfx)). Depends on the reader for the name set. (4) RELEASE DEAD-ASSET ELIMINATION + ASSET REPORT — the P5 dead-code gate applied to content (§5): release strips any asset no handle references and emits assets.report.txt byte-matching the example; a release-mode reference-graph walk over the emitted handle set, downstream of seam emission (needs handle references for reachability); dev mode bakes the dirty subgraph and never strips. (5) engine.assets SURFACE ADMISSION + GOLDEN INTEGRATION — admit engine.assets to funpack/surface.odin with EXACTLY the §26 line-78 names: types MeshHandle/TextureHandle/SoundHandle/AtlasHandle, fns mesh/texture/sound/atlas/cell/frame — the shared sink Modeling (§16) and UI (§21) import. A TYPING concern riding the existing import grammar (a new STDLIB_SURFACE partition + handle record schemas for record_check), NOT new grammar. Lands last: the .gen.fun seam and src/pickups.fun must typecheck handle expressions against it, and it carries the examples/assets golden pinning assert assets.coin_sfx == sound(coin_sfx). CRITICAL SCOPE: src/pickups.fun also references engine.render.Flip/Draw::Sprite and engine.audio.{Sound,Bus} — surface this epic does NOT own; the golden pins parse + the asset-specific assertion + the .gen.fun seam typecheck, and full src/pickups.fun end-to-end typecheck is gated on render/audio surface landing (a cross-epic edge, the driver's job). Rejected: (a) one mega story — not independently shippable; (b) folding the reader into importers — the hasher hashes OVER importer outputs and the manifest is the registry the emit/check seam consults, so reader-first is the dependency order; (c) folding strip into emission — strip is a release reference-graph walk needing emitted handle references, dev never strips, a downstream gate not an emission concern; (d) splitting surface from golden — the engine.assets partition is small (4 types + 5 fns) and the golden cannot typecheck the seam or assert without it.
- [11] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: Decomposing the §17 .flvl levels-bake epic surfaced a cross-cutting prerequisite the epic description did NOT name: the funpack frontend is single-source end-to-end. resolve_imports(ast) runs over ONE Ast; the resolver only knows STDLIB_SURFACE (engine.*); stage_emit/run_test_pipeline/emit_tree_artifact all key on project.sources[0]; collect_sources walks only src/ (so levels/ and gen/ are uncollected). The arena example fundamentally requires multi-module USER-module resolution — arena_game.fun imports BOTH arena_world (schema) and arena (the generated seam), and any import arena_world.{Player} currently returns .Unknown_Module. This is in-epic foundational work, NOT a discovery against another epic: shared-bake-infra provides the bake-step/emitter/golden-harness/seam-import-path, but says nothing about user-module-to-user-module import resolution, which is squarely funpack-team frontend scope. The levels epic decomposes as a fixed seam chain: (1) multi-module user resolution [unnamed prerequisite, provable on arena .fun sources alone, no .flvl] -> (2) engine.world.Ref + engine.nav surface admission [pure surface.odin table growth, §08 Ref + Nav/Path/NavError, precondition for arena_world/arena_game typing] -> (3) .flvl lexer+parser [new flat-text grammar, parse-only, hand-built fixtures] -> (4) .flvl resolver+bake gates [names->typed Ref[T], prefab/loop expansion, fixed-point anchor resolution, all §17.4 gates as compile errors] -> (5) golden byte-match arena.gen.fun + arena green E2E [cross-epic leaf consuming shared-bake-infra]. The .flvl grammar is a THIRD config-class grammar distinct from .fun and the .fcfg smaller-config grammar (lex_fcfg): it has expressions (offset arithmetic, for-range bounds) .fcfg lacks, so it needs its own lexer/parser pair, mirroring why .fcfg did not reuse the .fun lexer. Rejected: folding multi-module resolution into the bake story (independently shippable on existing arena .fun files; gating it behind .flvl would hide the single-source break); folding Ref/nav surface into the parser story (surface growth is a self-contained table edit; the .flvl parser needs no schema); splitting lexer from parser (the grammar is small, lexer's only consumer is its parser); a discovery on multi-module resolution (in funpack scope, not external).
- [10] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: Shared bake infrastructure (epic shared-bake-infrastructure-bui-mq2p3fbi, milestone bake-pipeline-seams) decomposes along four mechanism seams with this acyclic build order. (1) builds.fcfg + capability-derivation reader extends funpack/project.odin — adds a builds.fcfg parser production over the existing lex_fcfg/Cfg_Parser AND the §14.4 derived-capability tree-walk (a non-empty levels/ models/ ui/ assets/ subsystem dir => capability ON => its committed gen/*.gen.fun is expected). Pure config+tree reading; root, no in-epic deps. (2) gen/*.gen.fun canonical emitter is a NEW file producing the @doc-headed, formatter-canonical, diffable byte shape the committed goldens use (exemplar funpack-spec/examples/arena/gen/arena.gen.fun: file-leading @doc, import engine.<schema>.{…} + import <schema_module>.{…}, @doc-headed data/extern fn decls). Takes an in-memory seam model -> bytes; root, no in-epic deps; byte-shape TARGET is the committed exemplar. Distinct from emit.odin (binary artifact): this is source-text emission, the byte contract every downstream pipeline emitter reuses. (3) golden seam-comparison harness diffs freshly-emitted seam bytes against the committed gen/*.gen.fun, mirroring golden_emit_test.odin byte-equality + report_first_byte_diff and the resolve_spec_dir env-override/SKIP-warn resolution; a stale committed seam is a build error. Blocked by BOTH (1) and (2). (4) seam-import path through read_project: a baked gen/ seam joins the source set, derives its §15 module, rides lex->parse->gates->typecheck->contracts->flatten->evaluate like any .fun, honoring §17 schema/seam/behavior acyclic layering (a .gen.fun importing a behavior module is a compile error). Blocked by (1) only; consumes committed bytes as-is, independent of emitter/harness. Rejected: one mega 'bake infrastructure' story (the four mechanisms have distinct seams and a real dep-order); folding the emitter into the harness (the emitter is a reusable byte contract for all four authoring-source pipelines, independently shippable and double-emit-deterministic on its own). No new engine.* stdlib module — compiler-side plumbing only; honors spec §29 purity (no clock/host nondeterminism in funpack/**).
- [6] ct-language-lead-seat-f56cc7d6-d603-42e9-832b-6ce8313132c8: The funpack frontend decomposes along a fixed seam order — lexer/parser -> resolve/type -> contract/gate -> golden integration — and each seam is split so it is independently shippable behind the existing test harness; the parser-vs-typer boundary is the natural story cut. Two pong-surface checks are DISTINCT stages, never one: the per-behavior NODE check (a behavior is well-formed for its slot — Update/Render/Startup, spec §06) and the cross-behavior EDGE check (effect closure — every emitted signal has a downstream consuming stage, §04/§07). The edge check requires the depth-first flattened pipeline as the total order, so pipeline flattening is a prerequisite seam for effect closure and lands together with it, downstream of the node check. Read-side surface (§08 View[T]/first/fold) and bindings/setup (§23 bindings()/setup()) are TYPING concerns that ride the existing expression grammar (calls, members, lambdas, lists) once surface.odin admits the new engine.* modules — they are not new grammar. Typing a behavior body (blackboard reads-as-params/writes-as-return §06 §3; closed command/signal return types §04 §1 — Spawn/Draw/[Goal]; Input/Time resource params) is the node check's precondition, kept separate from the node check itself. Rejected: one mega 'parse + type + check pong' story (not independently shippable, and the parser/typer seam is the existing frontend boundary); folding node-check and edge-check into one 'contracts' story (the spec separates them, the edge check needs the flattened pipeline the node check does not, and a node-check-only story ships slot well-formedness on its own). Consolidates lore #1.
